# 家宴 · 家庭菜谱 PWA —— 高保真交互原型设计任务

## 产品背景
为一个两人家庭使用的「本地优先」菜谱 PWA 做高保真交互原型（产品名：家宴）。
菜谱库为 1190+ 道真实家庭中餐菜谱（来源已整理，应用内只读）。真实使用场景：
丈夫用「烹饪设备」找菜、做饭、排一周菜单、买菜；妻子用「选择设备」浏览、
收藏"想吃"的菜，并把选好的菜通过一段可粘贴的文字"点菜码"发给丈夫，由烹饪设备导入。

## 交付物
一个交互式 HTML 原型（单文件或多文件均可）：无后端、无网络依赖、双击即可打开。
移动优先（参考 390×844 视口），同时适配桌面 1440 宽（左侧紧凑侧边栏）。
所有交互用原生 JS 演示即可，不接真实数据、不做真实持久化。

## 界面清单（7 个主要界面 + 导航）

1. **找菜（首页）**
   - 顶部搜索框（占位提示：支持菜名 / 食材 / 做法，模糊与拼音首字母）。
   - 筛选行：做法（炒 / 炖 / 蒸 / 煮 / 烤 / 凉拌）、时长、减脂友好、热量与蛋白高低。
   - 菜谱卡片列表：菜名、标签、时长、热量、蛋白质、右上角心形（想吃 / 收藏）。
2. **菜谱详情**
   - 标题 + 标签 + 时长；份量步进器（默认 2 人份，数值按比例重算用量）。
   - 用料按分类分组（主料 / 辅料 / 调料），数量 + 单位；文字量（如"适量"）保持不变。
   - 步骤编号列表，含步骤定时器标注（例如"焖 15 分钟"）。
   - 营养卡片：热量 / 蛋白质 / 碳水 / 脂肪；个别菜显示缺省态"暂无估算"。
   - 来源与作者：有才显示。
   - 操作：加入午餐 / 加入晚餐、开始烹饪。
3. **烹饪模式**
   - 全屏，当前一步大字号显示；相关用料以标签（chip）展示。
   - 可选步骤倒计时（不自动开始计时）；上一步 / 下一步；顶部进度"第 2 步，共 8 步"。
   - 桌面宽度下左侧常驻完整用料清单。
4. **一周菜单**
   - 周一至周日 × 午餐 / 晚餐 网格，可向格子添加菜品。
   - 顶部营养汇总条：热量 / 蛋白质 / 碳水 / 脂肪；无营养数据的菜标注"暂无估算"。
   - 早餐 / 加餐可折叠展开。
5. **采购清单**
   - 按买菜分类分组（蔬菜、肉类、水产、豆制品、菌菇、其他）。
   - 一行 = 用品名 + 数量 + 单位；勾选 = 已购（划线并沉底）。
   - 底部一个手动添加输入框；界面提示默认已排除油盐酱醋等常备调料。
6. **点菜（选择设备）**
   - 想吃清单 → "生成点菜码"输出文本（格式如：`家宴点菜: {"recipeIds":["hong-shao-rou"]}`）＋复制按钮。
   - 导入页：粘贴点菜码 → 识别结果列表 → 加入待办；无效文本给出友好错误且不改变任何数据。
7. **设置**
   - 设备模式切换（烹饪设备 / 选择设备），说明两种模式差异。
   - "数据仅保存在本机"声明；关于 / 版本信息。

**导航结构**：移动端底部 Tab（找菜 / 菜单 / 采购 / 设置），注意 iOS 底部安全区
（safe-area-inset-bottom）；选择设备模式仅显示（找菜 / 想吃 / 点菜）。
桌面端为紧凑左侧边栏 + 内容区。

## 视觉方向（最重要）
克制、安静、有呼吸感的中文界面（苹果 HIG 风格启发，但绝不做苹果仿冒）：

- 背景暖白（接近 #FAFAF7），卡片纯白，近黑文字（#1C1A17 附近），次要文字暖灰；
  蓝色作为唯一强调色（约 #2F6FEB），每屏只出现一两处强调。
- 字体：系统字体栈 `-apple-system, "SF Pro Text", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", system-ui, sans-serif`，标题用同栈加粗；不加载外部字体。
- 圆角 10–14px；阴影极轻；间距以 8px 为基准；触控目标 ≥44px；对比度符合 WCAG AA。
- 卡片列表克制统一；避免花哨渐变、玻璃拟态、3D、营销页式大 hero；
  禁止出现 Apple 商标、苹果产品名或仿 Apple 官网排版。
- 产品文案用自然口语化的中文，少用感叹号与 emoji；图标用简洁线条风格（内联 SVG）。
- 示例菜品内容要真实可信：干炒牛河、红烧肉、清蒸鲈鱼、番茄炒蛋、麻婆豆腐、
  酸辣土豆丝、冬瓜排骨汤、蒜蓉西兰花、白灼虾、土豆炖牛肉等；
  时长与热量给合理数值，个别菜显示"暂无估算"。

## 明确不做
账号、登录、云端同步、真实数据库、后端请求、图片素材依赖、AI 识别菜谱。